#include "main.h"

/**
 * print_line - a function that draws a straight line
 * @n: the number of printed lines
 *
 * Return: 0 Always
 */
void print_line(int n)
{
	int count = n, i;

	for (i + 0; i < count; i++)
	{
		_putchar('_');
	}
	_putchar('\n');
}
