#include <stdio.h>
/**
 * main - Entry point
 *
 * a program that prints all single
 * digit numbers of base 10 starting from 0
 *
 * Return: Always 0 (correct)
 */
int main(void)
{
	int i;

	for (i = 48; i <= 57; i++)
	{
		putchar (i);
	}

	putchar ('\n');

	return (0);

}
